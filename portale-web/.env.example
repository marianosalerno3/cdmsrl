# Base URL dell'API. In dev di norma si usa il proxy Vite (/api -> localhost:8000),
# quindi puoi lasciare "/api". In produzione: https://api.<brand>/api
VITE_API_BASE_URL=/api

# Target del proxy Vite in sviluppo (solo dev).
VITE_API_PROXY=http://127.0.0.1:8000
